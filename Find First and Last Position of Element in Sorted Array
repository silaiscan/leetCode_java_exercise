class Solution {
    public int[] searchRange(int[] nums, int target) {

        int[] dizi = {-1, -1};

        // İlk pozisyonu bul
        int left = 0;
        int right = nums.length - 1;

        while (left <= right) {
            int mid = left + (right - left) / 2;

            if (nums[mid] >= target) {
                if (nums[mid] == target) {
                    dizi[0] = mid;
                }
                right = mid - 1;
            } else {
                left = mid + 1;
            }
        }

        // Son pozisyonu bul
        left = 0;
        right = nums.length - 1;

        while (left <= right) {
            int mid = left + (right - left) / 2;

            if (nums[mid] <= target) {
                if (nums[mid] == target) {
                    dizi[1] = mid;
                }
                left = mid + 1;
            } else {
                right = mid - 1;
            }
        }

        return dizi;
    }
}
